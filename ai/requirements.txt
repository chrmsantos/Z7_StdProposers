# Z7_StdProposers — Python dependencies
# Install with: py -3 -m pip install -r requirements.txt
# Or use install_requirements.bat for guided setup with validation.

google-genai>=1.0.0
pywin32>=306
Pillow>=10.0.0
pydantic>=2.0.0
